1 
docker-compose up -d --build 
docker-compose exec app bash


docker ps 
#запустить на localhost:8000 в контейнере (перейти в контейнере в папку project)
php -S 0.0.0.0:8000 -t public


composer require symfony/cache
composer require symfony/security-bundle
composer require twig

sudo chown -R $USER:$USER amazon-test
sudo chmod 755 -R amazon-test


user@example.com:
# пароль: test123


